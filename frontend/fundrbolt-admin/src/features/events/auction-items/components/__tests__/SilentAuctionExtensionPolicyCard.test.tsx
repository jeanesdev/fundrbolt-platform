import { describe, expect, it } from 'vitest'
import { SilentAuctionExtensionPolicyCard } from '../SilentAuctionExtensionPolicyCard'

describe('SilentAuctionExtensionPolicyCard', () => {
  it('is defined', () => {
    expect(SilentAuctionExtensionPolicyCard).toBeDefined()
  })
})
