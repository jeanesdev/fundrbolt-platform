name: Deploy Donor PWA

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'frontend/donor-pwa/**'
      - 'frontend/shared/**'
      - '.github/workflows/donor-pwa-deploy.yml'

permissions:
  contents: read
  id-token: write

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://give.fundrbolt.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        working-directory: ./frontend/donor-pwa
        run: pnpm install --frozen-lockfile

      - name: Build application
        working-directory: ./frontend/donor-pwa
        run: pnpm build
        env:
          VITE_API_URL: https://api.fundrbolt.com
          VITE_ENVIRONMENT: production
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN_DONOR }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: fundrbolt-donor

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DONOR_PROD }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: '/frontend/donor-pwa'
          output_location: 'dist'
          skip_app_build: true

      - name: Verify production deployment
        run: |
          for i in $(seq 1 6); do
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://give.fundrbolt.com || echo "000")
            echo "  Attempt $i/6: HTTP $HTTP_STATUS"
            if [[ "$HTTP_STATUS" == "200" ]]; then exit 0; fi
            sleep 15
          done
          echo "Deployment verification failed."
          exit 1
